package org.safa.maintenanceservice.models.model;

public enum LaborStatus {
    Busy, // if a labor id working
    Free, //labor is free to work
    Offline // displayed when closes the app completely
}
